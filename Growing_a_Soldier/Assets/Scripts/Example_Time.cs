using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;
public class Example_Time : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        DateTime.Now.ToString("yyyy");

    }

    // Update is called once per frame
    void Update()
    {
    }
}
